Tested on Windows 7.

This application expects that you installed MongoDB to C:\mongodb.

The batch file creates three database servers and sets them up as replicators.
The C# creates a database and a collection, inserts and updates some data, 
and has some performance measurement built in.
