Tested on Windows 7.

This application expects that you installed MongoDB to C:\mongodb.
The only thing that depends on mongodb being in that location is the batch file that runs mongod.exe.

It creates its own database and fills it with data.
